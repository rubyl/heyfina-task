# Cyto task set up

1. Clone repo
2. yarn install packages for both `client` and `server`
3. Start server with `yarn start`. Make sure to have server running before building client.
4. Start client with `yarn start`
